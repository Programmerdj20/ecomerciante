---
export interface Props {
	variant?: 'primary' | 'secondary' | 'outline';
	size?: 'sm' | 'md' | 'lg';
	href?: string;
	class?: string;
}

const { 
	variant = 'primary', 
	size = 'md', 
	href,
	class: className = '' 
} = Astro.props;

const baseClasses = 'inline-flex items-center justify-center font-medium transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-offset-2';

const variants = {
	primary: 'bg-brand-success hover:bg-brand-success-dark text-white focus:ring-brand-success/50 shadow-lg hover:shadow-xl font-semibold',
	secondary: 'bg-astro-surface hover:bg-astro-hover text-astro-text-primary focus:ring-astro-border border border-astro-border hover:glow-blue',
	outline: 'border-2 border-brand-primary/30 text-white hover:bg-brand-primary/10 hover:border-brand-primary/50 focus:ring-brand-primary/20 backdrop-blur-sm'
};

const sizes = {
	sm: 'px-3 py-1.5 text-sm rounded-md',
	md: 'px-6 py-3 text-base rounded-lg',
	lg: 'px-8 py-4 text-lg rounded-lg'
};

const classes = `${baseClasses} ${variants[variant]} ${sizes[size]} ${className}`;
---

{href ? (
	<a href={href} class={classes}>
		<slot />
	</a>
) : (
	<button class={classes}>
		<slot />
	</button>
)}